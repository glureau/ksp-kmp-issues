pluginManagement {
    plugins {
        id("com.google.devtools.ksp") version "1.6.10-1.0.2"
    }
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'ksp-kmp-issues'
include "annotations"
include "compiler"
include "mymodule"

